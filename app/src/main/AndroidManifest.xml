<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:theme="@style/AppTheme" android:label="VulkanScope" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="false" android:supportsRtl="true">
        <activity android:name=".RestartActivity" android:exported="false" android:process=":restart" android:taskAffinity="com.efishell.vulkanscope.restart" android:excludeFromRecents="true" android:noHistory="true" android:theme="@style/RestartTheme" />
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
