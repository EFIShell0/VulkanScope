# VulkanScope 0.41.16 native/Kotlin compile-correctness audit

- Corrected `DeviceExtensionEnumeration.status` comparison to use `std::strcmp`, avoiding forbidden pointer comparison against the `"available"` literal under `-Werror`.
- Moved `hdrTypesText(DisplayReport)` to top-level scope so the top-level TXT and HTML serializers can resolve it.
- Preserved 0.41.15 enumeration and HDR provenance behavior without changing query or report semantics.
- Added release-verifier gates for both compile regressions.
