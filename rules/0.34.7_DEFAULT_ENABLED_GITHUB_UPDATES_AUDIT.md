# VulkanScope 0.34.7 default-enabled GitHub updates audit

- Fresh-install preference fallback changed from `false` to `true`.
- Persisted `direct_updates_enabled=false` remains authoritative for existing users.
- Startup update discovery runs only when the effective setting is enabled.
- Disabling the setting persists `false`, hides update UI and blocks manual update checks/downloads until re-enabled.
- Obtainium informational guidance and `obtainium-config.json` are retained.
- Add to Obtainium runtime action remains absent.
- Update download/install verification logic is unchanged.
- Vulkan capability collection/report/database paths are unchanged.
