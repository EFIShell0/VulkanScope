# VulkanScope 1.0.14 fallback-import compile and first-install update-intro audit

## Release identity
- Version: 1.0.14
- versionCode: 1014
- Immutable predecessor: VulkanScope 1.0.13
- Predecessor ZIP SHA-256: `025450cbd0e3a0a643d8d053a86a3938578b78aac7fb9428fa2a159de3a444db`
- Predecessor package census: 439 files
- Vulkan registry/header baseline: Vulkan 1.4.362 / header 362
- Companion Database: 1.0.2
- Submission schema: 2
- technicalReport schema: 3

## Observed regression
The supplied Windows `:app:assembleRelease` log reaches Kotlin compilation and fails at `MainActivity.kt:8688:99` because `FallbackAnalysisImportDialog` declares `onImport: (File) -> Unit` while the selected value passed by `let(onImport)` is a `FallbackAnalysisCandidate`.

This is a `REGRESSION` / release-blocking compile defect. The correction passes the selected candidate's validated `file` member to the existing File callback and does not change fallback scan roots, filename bounds, import validation, SAF routing or storage permissions.

## Requested usability refinement
The first-install informational banner shown when direct GitHub updates are disabled referenced GitHub/Obtainium with a generic `INFO` badge. It now uses the existing local GitHub source glyph in the Vulkan accent container. This is presentation-only and does not enable update checks, network access, APK download or installation.

## Upstream boundary
The canonical Khronos Vulkan Registry was rechecked on 2026-09-09 and remains Vulkan 1.4.362 dated 2026-09-04. No registry/header/query/native/report/schema change is required.

## Evidence contract
- Immutable 1.0.13 must fail the 1.0.14 verifier when version/release-record checks are skipped.
- 1.0.14 must pass the same compile-shape and intro-icon contract.
- Negative mutations must reject passing a `FallbackAnalysisCandidate` directly to the File callback, removal of the selected `.file` extraction, restoration of the generic INFO badge, loss of the GitHub glyph, and stale release identity.
- An unrelated changelog wording mutation must remain accepted as a false-positive control.
- Android assemble/lint/unit and real-device first-install rendering are separate evidence classes; they are PASS only when actually executed.
