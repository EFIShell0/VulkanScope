# VulkanScope 0.80.10 / Vulkan 1.4.362 audit

Immutable predecessor: VulkanScope 0.80.9, SHA-256 `48bc7d2d3e5aae3cb69437dc620d58191b266f341dcab3050f10180ed5c5f172`, 316 files.

The release advances the locked registry/header baseline to Vulkan 1.4.362 / header 362 using bundled vk.xml SHA-256 `cf31c965cf6e788697139601da0c7e02a75a9b6c7ac764e7641f5521ffd9da06` and Vulkan-Headers commit `ee2ec5fd83dafce291024683b50dc89219333076`.

Registry-derived queryable provider coverage is 304 extensions (299 stable + 5 provisional). The delta adds `VK_KHR_pipeline_library_group_handles` and `VK_VALVE_buffer_device_address_allocation_alignment`. Explicit query-catalog identity remains 110 structs / 104 groups; the new provider structures use validated generic pNext/field serialization.

The offline Encyclopedia is regenerated to 842 commands, 6248 VK_* tokens, 2461 Vk* types, 476 registered extensions and 50 VkResult values. Vulkan Video remains 47 bounded exact-profile capability queries. Schema 2 / technicalReport 3 are unchanged.

Build/runtime evidence classes are recorded separately and are never promoted to PASS without actual execution.

The 1.4.362 negative-mutation gate rejects registry provenance drift, missing new provider coverage, promoted KHR alias loss, VALVE field-serialization loss and Encyclopedia census drift, with an unrelated wording mutation as the false-positive control. Retained source/state/mutation gates remain mandatory. Android assemble/lint/unit and real-device execution remain separate evidence classes.
