# VulkanScope 0.80.9 explicit Details action / Database 0.39.23 audit

Immutable predecessor is VulkanScope 0.80.8, SHA-256 `2a5ee5358cef4c05a5c1c90040a23c6c012b5ba2183bdfb8e8eab1792a7b214c`, 311 files. Vulkan registry/header remains 1.4.361 / 361, with 302 validated Android-queryable providers, 110 implemented physical-device structs, 104 query groups and the retained 47-query Vulkan Video exact-profile census.

The demonstrated interaction gap is that 0.80.8 exposes a visible `Details` affordance while the surrounding Format and Extension cards also invoke the same detail destination. 0.80.9 removes detail mutation from the cards and binds selected Format/supported Extension/catalog Extension state only to the explicit `Details` control. The cards remain read-only TV browse/focus surfaces and all existing detail-dialog scroll/TalkBack/large-text behavior remains intact.

Failing-before-fix is provided by `tools/verify_detail_button_only_0809.py --skip-version` against immutable 0.80.8. The successor verifier, state machine and negative-mutation/false-positive controls are mandatory. `tests/golden/0.80.8_regression_contract.json` locks predecessor runtime bytes and permits only the demonstrated `MainActivity.kt` interaction change.

The companion Database is 0.39.23. New submissions require VulkanScope 0.80.3 or newer; the Database Encyclopedia uses the same locked Vulkan 1.4.361 reference family while remaining separate from runtime capability evidence. Historical Database reports remain readable and are not rewritten or rehashed.

Android build/lint/unit and real-device touch/TV/TalkBack activation are separate evidence classes and are not implied by source/regression PASS.
