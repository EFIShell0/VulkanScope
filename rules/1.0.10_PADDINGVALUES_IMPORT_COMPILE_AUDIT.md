# VulkanScope 1.0.10 PaddingValues import compile audit

## Release identity
- Version: 1.0.10 / versionCode 1010
- Immutable predecessor: VulkanScope 1.0.9
- Predecessor ZIP SHA-256: `5b3166c06138bb175a3c7368bd043c0e97ffa875780c8a179a6534b26a662a5b`
- Predecessor census: 396 files
- Vulkan registry/header baseline: Vulkan 1.4.362 / header 362
- Companion Database: 1.0.2
- Submission schema: 2 / technicalReport: 3

## Demonstrated failure
The user supplied a Windows `:app:assembleRelease` log from immutable VulkanScope 1.0.9. Native configuration/build completed for arm64-v8a, armeabi-v7a and x86_64, then Kotlin compilation failed at `MainActivity.kt:10199:26` with `Unresolved reference 'PaddingValues'`.

The source calls `PaddingValues(horizontal = 16.dp, vertical = 8.dp)` in `ExpressiveContainedIconTextButton`, while the corresponding `androidx.compose.foundation.layout.PaddingValues` type is not imported. This is a concrete `BUG`, `REGRESSION` and `TEST-GAP`; no runtime Vulkan behavior is implicated.

The libadrenotools C/C++ messages shown earlier in the user's log are warnings from the pinned third-party native dependency and are not the build-stopping failure. This release does not alter that dependency or suppress those warnings without separate upstream/runtime evidence.

## Upstream/specification boundary
The canonical Khronos Vulkan Registry was rechecked on 2026-09-08 and remains Vulkan 1.4.362 dated 2026-09-04. The compile repair needs no Vulkan registry/header/query update.

## Repair boundary
Only one runtime source edit is permitted: add the missing `androidx.compose.foundation.layout.PaddingValues` import in `MainActivity.kt`. The existing `contentPadding = PaddingValues(horizontal = 16.dp, vertical = 8.dp)` expression is retained exactly, so the 1.0.9 contained icon/text button spacing and interaction behavior do not change. `app/build.gradle.kts` changes only versionName/versionCode to 1.0.10/1010.

Native Vulkan code, AndroidManifest, resources, AdvancedAnalysis, registry/generated data, report serialization, Database transport/identity, storage/network behavior, ABI set and dependency pins remain immutable-1.0.9 bytes.

## Evidence plan
`tools/verify_paddingvalues_import_compile_1010.py` models the exact compiler prerequisite and, with release-record checks disabled, must fail immutable 1.0.9 because the import is absent. The fixed tree must pass. `tools/test_paddingvalues_import_compile_1010_negative_mutations.py` must reject import removal, a wrong import, loss of the target `contentPadding` use and stale release identity, while an unrelated changelog-only mutation must remain accepted.

`tests/golden/1.0.9_regression_contract.json` locks immutable 1.0.9 runtime/build-chain bytes and permits only the demonstrated MainActivity change plus release identity metadata. Full retained quality gates, package hygiene, clean-extract re-verification and deterministic dual-ZIP reproduction remain required.

## Android build evidence boundary
Immutable 1.0.9: user-supplied `:app:assembleRelease` = **FAIL** at `:app:compileReleaseKotlin`, `Unresolved reference 'PaddingValues'`.

Successor 1.0.10 Android assemble/lint/unit remains a separate evidence class. It is PASS only if a post-fix Gradle run actually completes successfully; otherwise it is `NOT EXECUTED` in this environment.
