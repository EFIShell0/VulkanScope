# VulkanScope 0.41.42 HTML state-presentation / registry-label audit

## Trigger
The supplied 0.41.41 real-device HTML report proved successful Surface recovery and raw UUID serialization, but its embedded stylesheet rendered `Available` with the same green treatment as `Supported`, and registry coverage used the ambiguous label `Report schema`.

## Immutable predecessor
- VulkanScope-0.41.41.zip
- SHA-256: `fb54101445963f65f289cadaa6acde75b575baa2958ac51732c02313707aee76`

## Classification
- USABILITY / REPORTING: semantic HTML states were not visually distinct.
- DATA-INTEGRITY / USABILITY: registry evidence label could be confused with Database schema 2 or technicalReport schema 3.

## Repair
- `Available` now uses a distinct blue HTML badge treatment while `Supported` remains green.
- `Unavailable` and `Not available` route to the unavailable state before negative support-state matching.
- Not applicable retains its own neutral treatment; Incomplete and Unknown remain distinct.
- Registry coverage uses `Registry report schema` consistently in UI/TXT/HTML presentation.
- No Vulkan collection, Surface, feature/property, evidence-row, schema, submission or endpoint semantics changed.

## failing-before-fix
`tools/verify_html_presentation_04142.py --root <immutable 0.41.41>`: expected FAIL. The predecessor shares Supported/Available CSS and retains the ambiguous registry label.

## Successor
The same verifier passes on 0.41.42. Negative mutations reject collapsing Available back into Supported styling, routing Not available to Unsupported, or restoring the ambiguous registry label.

## Evidence classes
- Source/static regression gate: PASS after final verification.
- Android assembleRelease: NOT EXECUTED in the packaging environment.
- Real-device 0.41.42 HTML export: NOT EXECUTED in the packaging environment.
