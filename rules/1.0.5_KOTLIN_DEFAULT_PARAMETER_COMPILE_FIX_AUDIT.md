# VulkanScope 1.0.5 Kotlin default-parameter compile-fix audit

## Immutable predecessor
- VulkanScope 1.0.4 / versionCode 1004
- ZIP SHA-256: `e12a7ffd3fcf40a8c6423cae19beb07a4f349abb1bed0d6cc45abbd25d9b5589`
- Extracted file census: 366

## Demonstrated failure
The user supplied a Windows `:app:assembleRelease` run that reaches `:app:compileReleaseKotlin` after all three native ABIs build. Kotlin rejects `MainActivity.kt` at the two Cancel-action call sites with both `Argument type mismatch: actual type is '() -> Unit', but 'Boolean' was expected` and `No value passed for parameter 'onClick'`. The libadrenotools diagnostics earlier in the log are warnings and are not the task-ending failure.

## Root cause and minimal repair
`ExpressiveTextButton` declares `label`, then defaulted `enabled: Boolean = true`, then required `onClick: () -> Unit`. Passing `onDismiss` as the second positional argument therefore targets the Boolean slot. Both affected calls now use the explicit named argument `onClick = onDismiss`. No component signature, UI state, update behavior or Vulkan/report logic changes.

## Upstream recheck — 2026-09-08
Kotlin's current functions documentation requires a later required argument to be named when an earlier defaulted parameter is omitted, except for the functional-last-parameter trailing-lambda syntax. Khronos latest Registry remains Vulkan 1.4.362 dated 2026-09-04. No dependency, registry or header update is required.

## Required evidence
The exact predecessor call shape is failing-before-fix evidence. The 1.0.5 verifier requires both named `onClick` calls and forbids the demonstrated positional form. A standalone Kotlin compiler probe models the same parameter signature and confirms the positional predecessor call fails while the named successor call compiles. Negative mutations restore each bad call independently; an unrelated audit wording mutation is the false-positive control. Immutable predecessor hashes remain locked by `tests/golden/1.0.4_regression_contract.json`.

## Evidence boundary
The user's 1.0.4 Android build is FAIL at Kotlin compilation. Any 1.0.5 Android assemble/lint/unit result remains NOT EXECUTED in this packaging environment unless those Gradle tasks actually run; source/regression/compiler-probe evidence does not substitute for the user's post-fix Android build.

## Executed evidence
- Immutable 1.0.4 failing-before-fix verifier: observed FAIL.
- 1.0.5 targeted source verifier: PASS.
- 1.0.5 negative mutations and false-positive control: PASS.
- Standalone Kotlin compiler probe: predecessor call FAIL with the demonstrated argument-binding class; successor named call PASS.
- Immutable 1.0.4 -> 1.0.5 regression contract: PASS with 91 locked production/build-chain paths.
- Retained Vulkan/spec/report/lifecycle/security/resource/UI/accessibility constituent verifiers and state models: PASS when rerun separately.
- Retained applicable negative-mutation suites: PASS when rerun separately; the historical update-dialog source oracle was successor-aware only for the named Cancel callback form and its behavioral hierarchy was not weakened.
- Android assemble/lint/unit in the packaging environment: NOT EXECUTED because Gradle 9.7.1 was not cached and `services.gradle.org` could not be resolved.
