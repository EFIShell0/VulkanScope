# VulkanScope 1.0.0 advanced analysis / evidence workflow audit

## Release identity
- Version: 1.0.0 / versionCode 1000
- Immutable predecessor: VulkanScope 0.80.15
- Predecessor SHA-256: `6eb4ef08059897b2d852d2173fd8b01b171eadf2bc84fae47bce79014088a3b6`
- Predecessor census: 338 files
- Vulkan registry/header: 1.4.362 / header 362
- Companion Database: 1.0.0
- Submission schema: 2 / technicalReport: 3

## Feature scope
1.0.0 adds the requested fourteen-part analysis/tooling expansion without changing canonical Vulkan collection or report semantics: evidence provenance/actions; global current-report search; guided/manual System↔Turnip A/B; query diagnostics with measured app-side dedicated-probe elapsed time; requirement resolution; capability-oriented format filtering; exact-profile Vulkan Video matrix; separate Surface+Display presentation evidence; custom bounded local minimum profiles; raw technicalReport tree/export; explicit fixed-origin Database compare; bounded private session history; per-field copy/share/watch/Encyclopedia actions; and common Material 3 Expressive primitives.

## Evidence boundaries
Registry dependency/reference data remains reference metadata. Runtime extension absence is negative evidence only with authoritative enumeration. Vulkan Video associations do not become codec-wide support. Surface + Android Display matching is labelled compatible evidence rather than an end-to-end HDR guarantee. Query timing is shown only where the current app process measured a dedicated probe; native/report timing that does not exist is reported as unavailable. Database compare is explicit, bounded and local after the GET completes.

## Storage/network bounds
- Session history: app-private, GZIP, max 8 sessions, max 8 MiB uncompressed and 4 MiB compressed per record.
- Watched tokens: existing max 256.
- Custom profiles: max 32 saved profiles, max 64 rules/profile; import/export via explicit SAF.
- Raw JSON visible leaves: 256; global search visible results: 160.
- Database compare response: max 2 MiB from the fixed official HTTPS Worker origin and validated 64-hex report id.
- No new automatic/background report upload or third-party network endpoint.

## Non-regression
Native collectors, locked registry/generated query files, report serialization, Database submission envelope, Turnip archive safety, updater verification, Surface lifecycle and resource ceilings remain predecessor bytes under the immutable regression contract.

## Verification plan
The 1.0.0 verifier checks all fourteen requested surfaces and the evidence/storage/network boundaries. Its state model covers requirement tri-state behavior, custom-minimum PASS/FAIL/UNKNOWN, history limits, Database id/size fail-closed behavior, A/B workflow stages, presentation compatibility labels, search/JSON bounds and timing semantics. Negative mutations remove representative contracts one at a time and must be rejected; unrelated changelog wording remains an accepted control.
